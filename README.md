# tapis-deployer-ansible


Run `ansible-playbook playbooks/backup_scripts.yml` to create the template scripts in /tmp/tapis-backups.

Add a `backups_dir` var to playbook to put the scripts somewhere else.


